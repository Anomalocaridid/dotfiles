# ~/.config/zsh/config.d/z_last.zsh
# Script to run last on zsh startup.

# load Powerlevel10k configuration
# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
[[ -f ~/.p10k.zsh ]] && source ~/.p10k.zsh
